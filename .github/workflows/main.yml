name: secret-example
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: print secret
      env:
        DOKER_USER: ${{secrets.DOCKERHUB_USER}}
      run: echo ${{ env.DOKER_USER }}
